/** Single soil fill shared by hero strip + stats track — one continuous earth. */
export const SOIL_COLOR = "#7C5538";

/** soil_one.svg silhouette (wavy top); used as a CSS mask, never as a second fill. */
export const SOIL_MASK = "url(/soil_one.svg)";
